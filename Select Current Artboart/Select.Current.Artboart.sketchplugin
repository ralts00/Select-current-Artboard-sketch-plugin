// Select current Artboard (cmd shift <)

var page = [doc currentPage],
    current_artboard = [page currentArtboard]

[page deselectAllLayers]

if (current_artboard != null) {
  [current_artboard select:true byExpandingSelection:true]
}
